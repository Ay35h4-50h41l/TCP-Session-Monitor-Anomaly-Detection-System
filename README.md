# TCP-Session-Monitor-Anomaly-Detection-System
A multi-threaded Python client-server system that simulates TCP communication using a custom protocol and detects real-time anomalies — retransmissions, out-of-order packets, invalid sequence behavior, session hijacking, and active packet injection (fake responses, TCP RST, spoofing)
